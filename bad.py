    # if isinstance(tokens[0], int):
    #     return [int(tokens[0])] + parse_helper(tokens[1:])
    # if isinstance(tokens[0], float):
    #     return [float(tokens[0])] + parse_helper(tokens[1:])
    # if isinstance(tokens[0], str):
    #     return [tokens[0]] + parse_helper(tokens[1:])